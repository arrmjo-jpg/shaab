<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Polls Subsystem
|--------------------------------------------------------------------------
| إعدادات التصويت العام ومنع التكرار. الطبقات المرتكزة على IP مُعطَّلة افتراضياً
| (تجنّب التطابقات الكاذبة على الشبكات المشتركة NAT) وتتطلّب ضبط TrustProxies.
*/

return [

    // ─── التصويت العام ───────────────────────────────────────────────
    'vote' => [
        // حدّ معدّل التصويت لكل فاعل/دقيقة (مقاومة إساءة).
        'rate_limit' => (int) env('POLLS_VOTE_RATE_LIMIT', 30),

        // سقف صارم لكل IP يكمّل حدّ الفاعل — حارس ضدّ تدوير X-Client-Id. 0 = مُعطَّل
        // (الافتراضي الآمن). يُطبَّع عبر AdClientIp (/64) ويتطلّب TrustProxies لعنوان صحيح.
        'per_ip_rate_limit' => (int) env('POLLS_VOTE_PER_IP_RATE_LIMIT', 0),
    ],

    // ─── منع التكرار ─────────────────────────────────────────────────
    'dedup' => [
        // مسار سريع NX قبل ضربة القاعدة (تحسين). قيد الفرادة (poll_id, voter_hash) في
        // القاعدة هو الضمانة الموثوقة دائماً.
        'fast_cache' => (bool) env('POLLS_DEDUP_FAST_CACHE', true),

        // ارتكاز هويّة الناخب على بادئة IP (Tier B) — أقوى ضدّ تدوير الهويّة لكنه يطوي
        // الشبكات المشتركة (NAT). false = ارتكاز على الفاعل (client-id، Tier A) — الافتراضي.
        'strict_ip' => (bool) env('POLLS_DEDUP_STRICT_IP', false),
    ],
];
